# Pinned exact versions — DESIGN.md §8.4 fix #6.
# A multi-year backtest can span days-to-weeks of wall-clock time across many
# short GitHub Actions runs. An unpinned dependency update landing mid-backtest
# would mean different segments train under subtly different library behavior,
# silently breaking reproducibility. Update deliberately, never implicitly.

Flask==3.0.3
gunicorn==22.0.0
psycopg2-binary==2.9.9
python-dotenv==1.0.1
requests==2.32.3
beautifulsoup4==4.12.3

# Data providers
yfinance==1.5.1

# Modeling (Phase 2/3)
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.0
scipy==1.13.1
arch==7.0.0          # GARCH modeling for the hedge sleeve, DESIGN.md §3

# Supabase client — removed 2026-07-15: never actually imported anywhere in
# the codebase (everything talks to Postgres directly via psycopg2 — see
# src/db.py and .env.example). Its `realtime` sub-dependency pinned
# websockets<13, which hard-conflicted with yfinance==1.5.1's
# websockets>=13 requirement. Re-add only if something starts actually
# using the supabase-py client.
